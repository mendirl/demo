spring:
  datasource:
    url: jdbc:tc:postgresql:latest:///dbtest
  test:
    database:
      replace: none
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
logging:
  level:
    root: trace